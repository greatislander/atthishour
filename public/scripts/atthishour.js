const elem = document.querySelector('.ticker');
const opts = {
  typeSpeed: 75,
  loop: true
};
malarkey(elem, opts).type('The first games of the National Hockey League are played in Montreal and Ottawa /// ')
  .type('Robert Borden’s new wartime elections act disenfranchises conscientious objectors /// ')
  .type('Half-a-million women vote for the first time in Canada’s federal election /// ')
